# assets

This folder holds the demo recording referenced by the main README.

`demo.gif` is generated from [`../demo.tape`](../demo.tape) using
[vhs](https://github.com/charmbracelet/vhs):

```bash
vhs demo.tape   # writes assets/demo.gif
```
